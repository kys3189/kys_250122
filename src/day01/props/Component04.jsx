import React from 'react';

const Component04 = ({name, num, val}) => {
    

    return (
        <div>
            <button onClick={val}></button>
        </div>
    );
};

export default Component04;